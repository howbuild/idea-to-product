# State Machine Rules

For each state:

- name
- meaning
- who can enter it
- allowed previous states
- allowed next states
- required reason
- visible UI state
- related event/log

Do not add a state without defining transition rules.
