# Preview Rules

- The preview should be understandable in a browser.
- Use enough spacing and readable text so the user can discuss the structure.
- Add a clear notice that this is a structural prototype.
- Do not present the preview as final UI.
- Use simple HTML and CSS only.
