# Domains

The core plugin does not include concrete domain packs.

This directory provides:

- `registry.example.yaml`
- `_template/`

Use this structure for external wrapper repos such as `construction-idea-to-product`.

## Rule

Domain packs are question candidates, not confirmed policies.

Do not treat domain content as product requirements until the user confirms it.
